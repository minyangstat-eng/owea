% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve.R
\name{appro_opt}
\alias{appro_opt}
\title{Solve for an optimal design from an information-vector model (fast path).}
\usage{
appro_opt(
  pp,
  wb,
  infor_vec_all = NULL,
  X = NULL,
  infor_vec = NULL,
  theta = NULL,
  max_iter = 100L,
  tol = 1e-06,
  verbose = FALSE
)
}
\arguments{
\item{pp}{criterion: 0 = D-optimality or 1 = A-optimality (only these two are
supported).}

\item{wb}{\eqn{v \times k} selection matrix \eqn{dg/d\theta}.}

\item{infor_vec_all}{\eqn{k \times N} information-vector matrix.}

\item{X}{\eqn{N \times d} design matrix (one row per candidate point).}

\item{infor_vec}{model function \code{infor_vec(x, theta)}.}

\item{theta}{parameter vector.}

\item{max_iter}{maximum outer iterations.}

\item{tol}{convergence tolerance on the directional derivative.}

\item{verbose}{print per-iteration progress.}
}
\value{
list with \code{index}, \code{weight}, \code{points} (if \code{X}
  given), \code{sensitivity}, \code{iter}, \code{value}, \code{design}.
}
\description{
Compatibility entry point of the information-vector package. Candidate points
may be given as a precomputed \eqn{k \times N} matrix \code{infor_vec_all},
or as a design matrix \code{X} with a per-point function \code{infor_vec} and
\code{theta}.
}
