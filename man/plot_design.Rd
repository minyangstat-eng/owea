% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_design}
\alias{plot_design}
\title{Plot a design returned by \code{\link{optimal_design}} or
\code{\link{exact_design}}.}
\usage{
plot_design(res, cov_names = NULL, main = "Design")
}
\arguments{
\item{res}{a result from \code{\link{optimal_design}} (uses \code{weights})
or \code{\link{exact_design}} (uses integer \code{counts}).}

\item{cov_names}{optional covariate labels (defaults to the support column
names, or \code{x1, x2, ...}).}

\item{main}{plot title.}
}
\value{
\code{NULL}, invisibly (called for the plot).
}
\description{
Draws a quick visual of the design: a stem plot of weight/count vs the single
covariate (one covariate), a bubble scatter with area proportional to
weight/count (two covariates), or a labelled bar per support point (three or
more). Uses base graphics only.
}
\seealso{
\code{\link{optimal_design}}, \code{\link{exact_design}}.
}
