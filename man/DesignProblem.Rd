% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/problem.R
\name{DesignProblem}
\alias{DesignProblem}
\title{Construct a design problem on a fixed candidate set.}
\usage{
DesignProblem(
  X,
  theta = NULL,
  info_matrix = NULL,
  p = 0L,
  subset = NULL,
  grad_g = NULL,
  xi0_points = NULL,
  xi0_weights = numeric(0),
  n0 = 0,
  n1 = 1,
  info_vector = NULL
)
}
\arguments{
\item{X}{\eqn{n \times N} numeric matrix of candidate design points (one per
row).}

\item{theta}{numeric parameter vector. Required only when the model function
is written as \code{function(x, theta)}, or when using \code{grad_g} or an
existing design; otherwise optional.}

\item{info_matrix}{information-matrix model, either \code{function(x)} or
\code{function(x, theta)}, returning the \eqn{k \times k} matrix (general
path). Supply this OR \code{info_vector}.}

\item{p}{criterion: 0 = D-optimality or 1 = A-optimality (only these two are
supported).}

\item{subset}{integer vector of parameter indices of interest (partial
parameters); a convenience alternative to \code{grad_g}.}

\item{grad_g}{function \code{theta -> v x k} matrix \eqn{dg/d\theta^T} for a
general differentiable function \eqn{g(\theta)}; default identity.}

\item{xi0_points}{\eqn{n_0 \times N} support of an already-run design
(multistage); \code{NULL} for a single-stage design.}

\item{xi0_weights}{weights of that existing design.}

\item{n0, n1}{sample sizes of the existing and the new stage.}

\item{info_vector}{information-vector model, either \code{function(x)} or
\code{function(x, theta)}, returning the length-k vector \eqn{f} (fast
path). Supply this OR \code{info_matrix}.}
}
\value{
an object of class \code{"DesignProblem"}.
}
\description{
The model enters through ONE per-point function: supply either
\code{info_matrix(x)} returning the \eqn{k \times k} Fisher information
matrix at \eqn{x} (the general path), or \code{info_vector(x, theta)}
returning the length-k information vector \eqn{f} (per-point information
\eqn{f f'}, the fast path).
}
