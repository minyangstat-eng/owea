% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve.R
\name{owea}
\alias{owea}
\title{Run OWEA on a fixed candidate set.}
\usage{
owea(
  prob,
  eps0 = 1e-06,
  max_outer = 2000L,
  verbose = FALSE,
  init_weight = 0.01,
  merge = FALSE,
  merge_atol = 0.01,
  init_method = "minmax",
  full_scan_every = "auto",
  warm_support = NULL,
  warm_weights = NULL
)
}
\arguments{
\item{prob}{a \code{\link{DesignProblem}}.}

\item{eps0}{stopping threshold on the directional derivative.}

\item{max_outer}{maximum number of outer iterations.}

\item{verbose}{print per-iteration progress.}

\item{init_weight}{accepted for compatibility (unused by the C++ engine).}

\item{merge}{if \code{TRUE}, merge neighbouring support points within
\code{merge_atol} after convergence (default \code{FALSE}).}

\item{merge_atol}{distance threshold for merging (used only if
\code{merge = TRUE}).}

\item{init_method}{initial-support strategy: \code{"minmax"} (default),
\code{"minmaxmedian"}, \code{"random"}, \code{"iboss"}, \code{"auto"}.}

\item{full_scan_every}{accepted for compatibility (the engine scans the whole
grid every iteration via one batched product).}

\item{warm_support, warm_weights}{optional warm-start design (snapped to the
candidate grid).}
}
\value{
list with \code{support}, \code{weights}, \code{criterion},
  \code{max_d}, \code{iterations}, \code{converged}.
}
\description{
Run OWEA on a fixed candidate set.
}
