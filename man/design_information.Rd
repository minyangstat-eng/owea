% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/info.R
\name{design_information}
\alias{design_information}
\title{Information matrix of a design (information-matrix model).}
\usage{
design_information(
  design,
  weights,
  info_matrix = NULL,
  theta = NULL,
  link = NULL,
  f = NULL,
  x = NULL,
  fx = NULL,
  xx = NULL,
  ff = NULL,
  intercept = TRUE,
  coding = "zero-sum",
  factor_levels = NULL,
  ncat = NULL
)
}
\arguments{
\item{design}{\eqn{m \times N} matrix of support points (one per row); a
length-m numeric vector is accepted for a 1-D design.}

\item{weights}{numeric vector of length m.}

\item{info_matrix}{information-matrix model, either \code{function(x)} or
\code{function(x, theta)}, returning the \eqn{k \times k} matrix. Omit when
a model spec (\code{link} + terms) is given instead.}

\item{theta}{parameter vector; required only when \code{info_matrix} is
written as \code{function(x, theta)}. For a model spec that needs
\code{theta} (all links but identity), a missing \code{theta} is drawn from
\eqn{N(0,1)} with a warning.}

\item{link, f, x, fx, ff, xx, intercept, coding, ncat}{optional formula-style model
spec (as in \code{\link{optimal_design}}, including \code{"multinomial"} /
\code{"cumulative"} with \code{ncat}); an alternative to \code{info_matrix}.}

\item{factor_levels}{optional integer vector marking factor columns of
\code{design}; when omitted with a model spec, factor columns are
auto-detected (columns whose values are all positive integers).}
}
\value{
the \eqn{k \times k} information matrix of the design.
}
\description{
Returns \eqn{M(\xi) = \sum_i w_i\, \mathrm{info\_matrix}(x_i)} from the
support points and weights of a design.
}
